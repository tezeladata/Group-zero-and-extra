// Template strings use `` - backsticks instead of regular ones
let text=`Hello World!`
console.log(text)
// Template Strings allow both single and double quotes inside a string
console.log(`It is also "the fastest production car ever"`)

// Template strings also allow multiline strings

